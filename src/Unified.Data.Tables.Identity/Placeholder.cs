namespace Unified.Data.Tables.Identity;

// Deleted in Task 2 once the real types land. Exists so Task 1 produces a
// buildable project without smuggling in half-ported code.
internal static class Placeholder;
